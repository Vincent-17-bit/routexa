# TRAFIQ

```
npm install
cp client/.env.example client/.env   # add VITE_MAPBOX_TOKEN
npm run dev            # client + server + admin
npm run dev:client      # client only, :5173
npm run dev:server      # server only, :4000
npm run dev:admin       # admin only, :5174
```
